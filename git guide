Git commands basics lang ito

git status
- check status and files

git add
- add files to stagging

git commit
- commit staged files and prepare to push

git push
- push the code changes to the repository



Steps to push
1. git pull  (always pull to avoid code conflicts)
--mag pull ka lagi para makuha mo yung latest code sa remote repo

2. git add (choose files to add, pwdeng all)
ex. git add .      (  . means all   )

3. git commit -m "message here"
ex. git commit -m "fix login bug"

4 git push